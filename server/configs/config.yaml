server:
  port: 8080
  mode: development

database:
  host: localhost
  port: 3306
  user: root
  password: ""
  dbname: what_cse
  charset: utf8mb4

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0

jwt:
  secret: "your-secret-key-change-in-production"
  expiration_hours: 24
  refresh_hours: 168

log:
  level: info
  format: json
  output: stdout
